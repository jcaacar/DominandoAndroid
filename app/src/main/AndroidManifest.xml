<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vm10.dominandoandroid" >

    <application
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Chapter 2 -->
        <activity android:name=".chapter2.MainActivity" />
        <activity android:name=".chapter2.SenderParcelableActivity" />
        <activity android:name=".chapter2.ReceiverParcelableActivity" />

        <!-- Chapter 4 -->
        <activity android:name=".chapter4.MainActivity" />
        <activity android:name=".chapter4.AdapterActivity" />
        <activity android:name=".chapter4.ExpandableListViewActivity" />
        <activity android:name=".chapter4.TextViewActivity" />
        <activity android:name=".chapter4.EditTextActivity" />
        <activity android:name=".chapter4.AutoCompleteTextViewActivity" />

        <!-- Chapter 5 -->
        <activity android:name=".chapter5.MainActivity" />
        <activity android:name=".chapter5.CustomViewActivity" />

    </application>

</manifest>